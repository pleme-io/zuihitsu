zuihitsu is installed.

Hashnode publication: {{ .Values.hashnode.host }}

{{- if .Values.ingress.enabled }}
Blog URL:
{{- range .Values.ingress.hosts }}
  https://{{ .host }}/
{{- end }}
{{- else }}
Port-forward locally:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "pleme-lib.fullname" . }} 3000:{{ (index .Values.service.ports 0).port }}
{{- end }}

Health:
  kubectl -n {{ .Release.Namespace }} logs -l app.kubernetes.io/name={{ include "pleme-lib.name" . }} -f
