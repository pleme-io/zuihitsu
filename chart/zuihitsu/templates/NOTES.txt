zuihitsu is installed.

Hashnode publication: {{ .Values.hashnode.host }}

{{- if .Values.ingress.enabled }}
Blog URL:
{{- range .Values.ingress.hosts }}
  https://{{ .host }}/
{{- end }}
{{- else }}
Port-forward locally:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "zuihitsu.fullname" . }} 3000:{{ .Values.service.port }}
{{- end }}

Health:
  kubectl -n {{ .Release.Namespace }} logs -l app.kubernetes.io/name={{ include "zuihitsu.name" . }} -f
