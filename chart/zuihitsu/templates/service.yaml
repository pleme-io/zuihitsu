apiVersion: v1
kind: Service
metadata:
  name: {{ include "zuihitsu.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "zuihitsu.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "zuihitsu.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
